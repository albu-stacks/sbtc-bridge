name: Build Docker Image for Environment Branch

on:
  push:
    branches:
      - 'environment/*'

concurrency:
  group: docker-build-${{ github.ref || github.run_id }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read
  attestations: write
  packages: write

env:
  docker_platforms: "linux/amd64"

jobs:
  image:
    name: Build Image
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    steps:
      - name: Configure AWS Credentials via OIDC
        id: configure_aws
        uses: stx-labs/actions/aws/configure-aws-credentials@ed353a33594a2fa97ea6702d838e4a935cce374d
        with:
          role-to-assume: ${{ secrets['AWS_ROLE_ARN'] }}
          aws-region: ${{ vars['AWS_REGION'] }}

      - name: Login to Amazon ECR
        id: ecr_login
        uses: stx-labs/actions/aws/amazon-ecr-login@ed353a33594a2fa97ea6702d838e4a935cce374d

      - name: Checkout Repository
        id: checkout_repository
        uses: stx-labs/actions/checkout@ed353a33594a2fa97ea6702d838e4a935cce374d

      - name: Docker Metadata
        id: docker_metadata
        uses: stx-labs/actions/docker/metadata-action@ed353a33594a2fa97ea6702d838e4a935cce374d
        with:
          images: |
            ${{ steps.ecr_login.outputs.registry }}/${{ vars['AWS_ECR'] }}
          tags: |
            type=raw,value=sbtc-bridge-${{ github.ref_name }}
            type=raw,value=sbtc-bridge-${{ github.sha }}
            type=raw,value=sbtc-bridge-latest,enable=${{ github.ref_name == 'environment/mainnet' }}

      - name: Build and Push
        id: docker_build
        uses: stx-labs/actions/docker/build-push-action@ed353a33594a2fa97ea6702d838e4a935cce374d
        with:
          file: ./Dockerfile
          platforms: ${{ env.docker_platforms }}
          tags: ${{ steps.docker_metadata.outputs.tags }}
          labels: ${{ steps.docker_metadata.outputs.labels }}
          push: true
          build-args: |
            GIT_COMMIT=${{ github.sha }}

      - name: Generate artifact attestation
        id: generate_attestation
        uses: stx-labs/actions/attest-build-provenance@ed353a33594a2fa97ea6702d838e4a935cce374d
        with:
          subject-name: ${{ steps.ecr_login.outputs.registry }}/${{ vars['AWS_ECR'] }}
          subject-digest: ${{ steps.docker_build.outputs.digest }}
